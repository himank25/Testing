#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>


int main()
{
	pid_t pid;
	pid=fork();
	
	if(pid<0)
	{
		printf("\n Forking failed");
		return 1;	
	}	
	
	else if(pid==0)
	{
		execl("/home/himank/Desktop","client"(.NULL);
	}

	return 0;
};
